language: java
jdk:
  - oraclejdk8

before_install:
 - chmod +x gradlew

install:
  - cd /tmp; git clone https://github.com/coinblesk/coinblesk-shared-resources.git; cd coinblesk-shared-resources; 
  - ./gradlew clean test
  - ./gradlew install
  - ./gradlew --version
  
script:
  - cd; cd coinblesk/coinblesk-server
  - ./gradlew --version
  - ./gradlew clean test