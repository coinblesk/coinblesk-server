dependencies {
    runtime 'com.h2database:h2'
}
task deploy << {
    def tomcatHome='/opt/tomcat'
    println "Copy new war"
    //without up-to-date check
    copy {
        from war.archivePath
        into "${tomcatHome}/webapps/"
    }
}